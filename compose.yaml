services:
  media-library-management-mcp-bun:
    container_name: media-library-management-mcp
    restart: unless-stopped
    ports:
      - 6600:6600
    build: .
    volumes:
      - .:/usr/src/app
    env_file:
      .env
