{
  "name": "media-library-management-mcp",
  "description": "An MCP server that can be used for media library files",
  "module": "out/index.js",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "prestart": "bun run build",
    "start": "bun .",
    "build": "bun build --target=bun --minify --outdir=out/ src/index.ts",
    "dev": "bun run --hot src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.20.2",
    "arkenv": "^0.7.3",
    "arktype": "^2.1.25",
    "zod": "3"
  }
}
